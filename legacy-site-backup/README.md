# Legacy root site backup

This folder preserves the original static website that formerly deployed at `https://nafisalmuhsin.com/`.

It is intentionally excluded from the active Astro build. To roll back, restore these files to the repository root and revert the root-site deployment configuration in the corresponding Git commit.
